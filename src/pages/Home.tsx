import Layout from '../Layout/Layout';
import Menu from '../components/Menu/Menu/Menu'

const Home = () => {
    return (
        <Layout>
            <Menu />
        </Layout>
    );
}

export default Home